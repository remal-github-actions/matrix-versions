name: Matrix versions
description: Retrieve dependency versions for matrix jobs

inputs:
  githubToken:
    required: true
    description: GitHub access token with at least 'public_repo' (use 'repo' scope instead of 'public_repo' for private repositories)

runs:
  using: node12
  main: 'dist/index.js'
